spring:
  profiles:
    active: develop

logging:
  level:
    root: INFO
    com.moebius: DEBUG
  file: /var/log/moebius/app.log

moebius:
  protocol: http
  host: dev-api.cryptoboxglobal.com
  web-security:
    all: /,
      /csrf,
      /static/**,
      /v2/api-docs,
      /swagger,
      /swagger-ui.html,
      /swagger-resources/**,
      /webjars/**,
      /api/member/**,
      /api/members/**,
      /login
    admin: /admin,
      /api/markets/**
    member: /api/stoplosses/**,
      /api/api-keys/**,
      /api/orders/**

reactor:
  netty:
    http:
      server:
        AccessLog: INFO
