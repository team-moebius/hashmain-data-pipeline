spring:
  profiles:
    active: production
  error:
    whitelabel:
      enabled: false

logging:
  level:
    root: INFO
    com.moebius: INFO
  file: /var/log/moebius/app.log

moebius:
  protocol: http
  host: api.cryptoboxglobal.com
  web-security:
    all: /,
      /csrf,
      /static/**,
      /api/member/**,
      /api/members/**,
      /login
    admin: /admin,
      /api/markets/**
    member: /api/stoplosses/**,
      /api/api-keys/**,
      /api/orders/**

reactor:
  netty:
    http:
      server:
        AccessLog: INFO