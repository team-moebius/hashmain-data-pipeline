import MuiTableCell from '@material-ui/core/TableCell';
import { withStyles } from '@material-ui/styles';

const TableCell = withStyles(theme => ({}))(MuiTableCell);

TableCell.defaultProps = {};

export default TableCell;
