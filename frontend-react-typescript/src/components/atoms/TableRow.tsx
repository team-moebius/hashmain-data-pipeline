import MuiTableRow from '@material-ui/core/TableRow';
import { withStyles } from '@material-ui/styles';

const TableRow = withStyles(theme => ({}))(MuiTableRow);

TableRow.defaultProps = {};

export default TableRow;
