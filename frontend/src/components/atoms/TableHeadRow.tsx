import MuiTableRow from '@material-ui/core/TableRow';
import { withStyles } from '@material-ui/styles';

const TableHeadRow = withStyles(theme => ({}))(MuiTableRow);

TableHeadRow.defaultProps = {};

export default TableHeadRow;
