import MuiToolbar from '@material-ui/core/Toolbar';
import { withStyles } from '@material-ui/styles';

const Toolbar = withStyles(theme => ({}))(MuiToolbar);

Toolbar.defaultProps = {};

export default Toolbar;
